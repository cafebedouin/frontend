import { Plus_Jakarta_Sans, Quicksand } from 'next/font/google';

export const PlusJakarta = Plus_Jakarta_Sans({
  subsets: ['latin'],
  weight: ['400', '500'],
});

export const Qiucksand = Quicksand({ subsets: ['latin'] });
